#!/usr/bin/env bash
#MISE description="Check the upstream TLA+ baseline with bounded and sampled smoke profiles"
set -euo pipefail

root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)"
exec python3 "$root/formal/check.py" smoke "$@"
